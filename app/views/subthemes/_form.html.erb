<%= form_for(@subtheme) do |f| %>
  <% if @subtheme.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subtheme.errors.count, "error") %> prohibited this subtheme from being saved:</h2>

      <ul>
      <% @subtheme.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :image %><br />
    <%= f.text_field :image %>
  </div>
  <div class="field">
    <%= f.label :video_url %><br />
    <%= f.text_field :video_url %>
  </div>
  <div class="field">
    <%= f.label :theme_id %><br />
    <%= f.number_field :theme_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
